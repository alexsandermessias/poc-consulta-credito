spring:
  profiles:
    active:
    - test

  config:
    import:
    - file:src/main/resources/application.yml

  flyway:
    enabled: false

kafka:
   bootstrapServers: localhost:9092
   topic: test-topic    
cors:
   origins: http://172.17.0.2   

 
